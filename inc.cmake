# template copied to each application directory as automoc will overwrite
# generated files with other applications' moc files if they are named the same
# and not kept in separate directories
include(${CMAKE_SOURCE_DIR}/app.cmake)
